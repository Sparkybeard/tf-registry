BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[GpgPublicKey] ALTER COLUMN [asciiArmor] NVARCHAR(4000) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
